## 已完成

- 重置的最后一分钟加个显示秒数的功能
- 时间到后自动刷新额度进度，不需要用户手动刷新浏览器

---

## TODO（按难度渐进）

> **总预估**（业余节奏、完全自己实现）：约 **3–5 周**  
> 建议按阶段顺序做，每完成一阶段再进入下一阶段。

---

### 第一阶段 · 入门（⭐）

熟悉 `ui.js` / `styles.css`，改动局部、反馈快。


| #   | 任务                | 预估      | 学习重点                                                                                                          |
| --- | ----------------- | ------- | ------------------------------------------------------------------------------------------------------------- |
| 1   | [ ] **插件加载展开动画**  | 2–4 小时  | 读 `attachHeader()` / `attachUsageLine()`，在 UI 首次挂载时加展开效果（`opacity` + `transform` 或 `max-height`）；注意 DOM 重挂载时别每次都播，可用「本页只播一次」标记 |
| 2   | [x] **倒计时抖动 bug** | 0.5–1 天 | 学 `font-variant-numeric: tabular-nums`、`min-width`；在 `tick()` 里对比新旧文本，值未变则不更新 DOM；用 DevTools Layout 观察 reflow |
| 3   | [ ] **额度三色进度条** | 2–4 小时 | 读现有 `cc-warn` 实现（≥90% 变红）；扩展为三段：0–33% 默认蓝、33–66% 黄、66%+ 红；在 `constants.js` 加黄色常量，`setUsage()` 按 `width` 切换 class，`styles.css` 加 `.cc-caution` 等 |
| 4   | [ ] **时间参数调试器** | 0.5–1 天 | 开发模式下可手动设置 `sessionResetMs` / `weeklyResetMs` / 缓存截止时间等，方便调试倒计时与窗口 rollover；可用 `localStorage` 开关 + 简单面板或 `console` API，勿影响正式用户 |


**阶段小结**：做完应能熟练改 header / usage 行的 DOM 和 CSS，理解 `tick()` 每秒刷新机制、UI 挂载时机，以及进度条颜色 class 体系。

---

### 第二阶段 · 进阶（⭐⭐）

局部 UI 增强 + 字符串整理，仍主要在 content script 内。


| #   | 任务                                   | 预估      | 学习重点                                                                                                                                               |
| --- | ------------------------------------ | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4   | [ ] **压缩后 token 变灰 + 删除线 + tooltip** | 0.5–1 天 | 读 `setConversationMetrics()` 里 `pct >= 99.5` 的现有逻辑；加 CSS 状态 class；tooltip 统一走 `LABELS[lang]`。进阶：抓包看对话 API 是否有 compaction 字段                        |
| 5   | [ ] **中英文切换开关**                      | 1 天     | 补全 `LABELS` 映射（`setUsage()` 里 Session/Weekly/resets in 等硬编码）；用 `chrome.storage.local` 或 `localStorage` 持久化；小开关 UI 可放在 usage 行旁。若维护 userscript 需同步改 |


**阶段小结**：掌握 `LABELS` 国际化模式、tooltip 体系、会话 metrics 与 UI 状态的对应关系。

---

### 第三阶段 · 中级（⭐⭐⭐）

需要状态判断或新产品模型，逻辑比样式更绕。


| #   | 任务                 | 预估    | 学习重点                                                                                                                                               |
| --- | ------------------ | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 6   | [ ] **用量增加高亮动画**   | 1–2 天 | 梳理 `setUsage()` 的调用来源（SSE / `/usage` / 手动点击）；维护 `lastPct + source + timestamp` 小状态机；区分「真增加 / 刷新 / 窗口重置」三种情况；动画用 CSS transition 或 brief flash class |
| 7   | [ ] **周用量 7 段进度条** | 1–2 天 | 先定产品模型（7 段 = 7 个自然日 or rolling 均分）；复用 `weeklyWindowStartMs` / `weeklyResetMs`；参考现有 `cc-bar` / `cc-bar__marker` 结构扩展 DOM                            |


**阶段小结**：能独立设计「何时更新 UI、何时触发动画」的状态流，不只看单次函数。

---

### 第四阶段 · 中高级（⭐⭐⭐⭐）

触及扩展新模块或外部数据源，调研成分高。


| #   | 任务                  | 预估          | 学习重点                                                                                                                                              |
| --- | ------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| 8   | [ ] **双倍额度提示**      | 0.5 天 ~ 3 天 | **轻量版**：静态说明或手动开关（半天）。**准确版**：DevTools Network 抓 Claude 页面/SSE/`/usage` 响应，找双倍额度字段；接受 Claude 更新后可能失效。建议先做轻量版                                      |
| 9   | [ ] **Popup 自定义分段** | 2–4 天       | 学 MV3：`action.default_popup` 或 `options_page`；`chrome.storage` 读写；popup 与 content script 通过 storage 事件或启动时读取同步；manifest 加权限。可先做固定 7 段（#7），再抽象为可配置 |


**阶段小结**：第一次扩展项目架构（新页面 + 持久化配置），理解扩展各上下文的分工。

---

### 第五阶段 · 高级（⭐⭐⭐⭐⭐）

跨域集成 + 语义模糊，投入大、收益不确定。


| #   | 任务                     | 预估    | 学习重点                                                                                                                                                             |
| --- | ---------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 10  | [ ] **Claude 服务器状态提示** | 3–7 天 | 调研 `status.claude.com` API 与 CORS；可能需 `host_permissions` + background service worker 代理；设计轮询间隔与缓存；明确「状态异常 ≠ 消息必丢」，做降级与误报控制。建议分步：① 仅只读展示 status 摘要 → ② 再考虑与发送行为联动 |


**阶段小结**：完成一项完整的外部集成，manifest 会从「极简 content script」变为「带 background 的扩展」。

---

## 推荐学习路径

```
跟通数据流（半天）
  main.js → bridge-client → bridge.js → ui.js
       ↓
第一阶段 #1 #2 #3 #4  （CSS + DOM + 进度条颜色 + 调试器）
       ↓
第二阶段 #4 #5  （i18n + 状态样式）
       ↓
第三阶段 #6 #7  （状态机 + 新 UI 模型）
       ↓
第四阶段 #8 #9  （调研 + MV3 新模块）  ← #7 可先做固定版，#9 再 configurable
       ↓
第五阶段 #10    （外部集成，可选/分阶段）
```

### 动手前建议通读的文件


| 文件                         | 为什么                 |
| -------------------------- | ------------------- |
| `src/content/ui.js`        | 几乎所有 TODO 的 UI 入口   |
| `src/content/main.js`      | 用量刷新、事件编排、`tick()`  |
| `src/styles.css`           | 进度条、动画、布局           |
| `src/content/constants.js` | 颜色、DOM 选择器          |
| `src/injected/bridge.js`   | 需抓 API / SSE 时从这里入手 |


### 自学资源（按需查阅）

- **CSS 等宽数字 / 动画**：MDN — `font-variant-numeric`、`@keyframes`、`transition`
- **DOM 性能**：减少 layout thrashing；更新前比较 `textContent`
- **MV3 扩展**：MDN WebExtensions — `storage`、`action`、`host_permissions`
- **调试 API**：Chrome DevTools → Network，过滤 `claude.ai/api`

